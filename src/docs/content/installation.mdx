---
title: "Installation"
description: "Learn how to install and set up the project"
related:
  - title: "Project Structure"
    href: "/docs/project-structure"
onThisPage:
  - title: "System Requirements"
    href: "#system-requirements"
  - title: "Set up the project"
    href: "#set-up-the-project"
---

Once you're done, you can check out the [Launch in 5 Minutes](/docs/tutorials/launch-in-5-minutes) tutorial to learn how to deploy your project in minutes.

Let's get started! 🚀

<br/>

<div id="system-requirements" />
### System Requirements

<ul className="ml-0.5 list-disc list-inside flex flex-col text-sm">
    <li>
        [Node.js (version 14 or higher)](https://nodejs.org/en/)
    </li>
    <li>
        [npm (version 6 or higher)](https://www.npmjs.com/) or [yarn (version 1.22 or higher)](https://yarnpkg.com/)
    </li>
</ul>

<br/>
<br/>

<div id="set-up-the-project" />
### Set up the project

Follow these steps to get Runway.js up and running on your local machine:

1. Clone the repository and install dependencies:

    ```bash
    `git clone git@github.com:silasnevstad/runway-js.git`
    `cd runway-js`
    `npm install`
    ```

    <Info>
        If you prefer using Yarn, you can replace `npm install` with `yarn install`.
    </Info>
    <br />
2. Rename `.env.example` to `.env.local`
    ```bash
    `mv .env.example .env.local`
    ```

<Caution>
    Make sure to add your own environment variables to `.env.local` before running the project.
</Caution>

<br />

3. Open <InlineHighlight type="navigation">http://localhost:3000</InlineHighlight> and done!

<br />
<br />
<br />
