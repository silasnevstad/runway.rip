---
title: "Select"
description: "A select component that can be used to choose from a predefined set of options."
onThisPage:
  - title: "Select"
    href: "#select"
  - title: "Usage"
    href: "#usage"
  - title: "Props"
    href: "#props"
  - title: "Examples"
    href: "#examples"
---

<div id="select" />

<br/>

<div className="flex flex-col items-center gap-5">
    <Select
        label="Select an option"
        labelMode="float"
        labelBackground="bg-bg-0 dark:bg-gray-900"
        options={[
            { value: "optionA", label: "Option A" },
            { value: "optionB", label: "Option B" },
        ]}
        size="md"
    />
    <Select
        label="Select an option"
        labelMode="inside"
        options={[
            { value: "optionA", label: "Option A" },
            { value: "optionB", label: "Option B" },
        ]}
        size="md"
    />
</div>

<br />

The `Select` component is built using the native <code>&lt;select&gt;</code> element,
and supports multiple label styles, various sizes, coloring, and more.

<br/>
<br/>
<Divider />

<div id="usage" />

## Usage

```js
import Select from "@/components/atoms/Select";
import { useState } from "react";

export default function Example() {
    const [value, setValue] = useState("");

    const handleChange = (e) => {
        setValue(e.target.value);
    };

    return (
        <div>
            <Select
                label="Pick something"
                labelMode="above"
                options={[
                    { value: "red", label: "Red" },
                    { value: "blue", label: "Blue" },
                ]}
                value={value}
                onChange={handleChange}
            />
        </div>
    );
}
```

<br/>
<br/>
<br/>
<Divider />

<div id="props" />

## Props

The `Select` component accepts the following props:

| Name               | Type                                     | Default                  | Description                                                                                           |
|--------------------|------------------------------------------|--------------------------|-------------------------------------------------------------------------------------------------------|
| id                 | string                                   | `""`                     | The `id` for the underlying <code>&lt;select&gt;</code> and corresponding <code>&lt;label&gt;</code>. |
| label              | string                                   | `""`                     | The text for the label.                                                                               |
| labelMode          | "none" \| "inside" \| "above" \| "float" | `"none"`                 | How to display the label: `"none"`, `"inside"` `"above"`, or `"float"`.                               |
| labelBackground    | string                                   | `bg-bg-0 dark:bg-bg-900` | Background color used behind the floating label.                                                      |
| size               | string                                   | `"md"`                   | Size of the component: `"sm"`, `"md"`, `"lg"`.                                                        |
| borderRadius       | string                                   | `"md"`                   | Tailwind border-radius classes (e.g. `"md"`, `"lg"`, `"full"`).                                       |
| color              | string                                   | `"gray"`                 | Used for text and border color (e.g. `"gray"`, `"primary"`, `"blue"`).                                |
| shadow             | string                                   | `""`                     | Tailwind shadow classes (e.g. `"md"` for `shadow-md`).                                                |
| focus              | boolean                                  | `true`                   | If `true`, shows focus style (border and ring).                                                       |
| activeColor        | string                                   | `"primary"`              | Color for the focus ring (e.g. `"primary"`, `"blue"`, etc.).                                          |
| includeEmptyOption | boolean                                  | `true`                   | If `true`, includes an empty option at the top of the dropdown.                                       |
| value              | string                                   | `""`                     | Controlled value of the select.                                                                       |
| onChange           | function                                 | -                        | Callback fired when the value changes. Receives the event as an argument.                             |
| options            | Array                                    | `[]`                     | An array of objects with shape `{ value, label }` for the dropdown.                                   |
| className          | string                                   | `""`                     | Additional [Tailwind](https://tailwindcss.com) classes for the container.                             |
| textClassName      | string                                   | `""`                     | Additional classes for the <code>&lt;select&gt;</code> itself (e.g. text color, font style, etc.).    |
| ...props           | object                                   | -                        | Other valid attributes for a <code>&lt;select&gt;</code> (e.g. `disabled`, `multiple`, etc.).         |

<Tip>
    The `Select` component is built using the native <code>&lt;select&gt;</code> element, so it supports all the same attributes.
</Tip>

<br/>
<br/>
<br/>
<Divider />

<div id="examples" />

## Examples

### Basic Usage
Use different label modes (`above`, `float`, `inside`, `none`), colors (`color`), and sizes (`sm`, `md`, `lg`) to customize the look and feel.

<PreviewCode preview={
    <div className="flex flex-col gap-5 min-w-80 items-center">
        <Select
            label="Label Above"
            labelMode="above"
            color="primary"
            options={[
                { value: "banana", label: "Banana" },
                { value: "apple", label: "Apple" },
            ]}
        />
        <Select
            label="Floating Label"
            labelMode="float"
            labelBackground="bg-bg-0 dark:bg-gray-900"
            color="blue"
            options={[
                { value: "blue", label: "Blue" },
                { value: "red", label: "Red" },
            ]}
            includeEmptyOption
        />
        <Select
            labelMode="none"
            placeholder="Pick a size"
            size="sm"
            color="green"
            options={[
                { value: "s", label: "Small" },
                { value: "m", label: "Medium" },
                { value: "l", label: "Large" },
            ]}
        />
    </div>
}>
    ```js
    <Select
        label="Label Above"
        labelMode="above"
        color="primary"
        options={[
            { value: "banana", label: "Banana" },
            { value: "apple", label: "Apple" },
        ]}
    />

    <Select
        label="Floating Label"
        labelMode="float"
        color="blue"
        options={[
            { value: "blue", label: "Blue" },
            { value: "red", label: "Red" },
        ]}
        includeEmptyOption
    />

    <Select
        labelMode="none"
        placeholder="Pick a size"
        size="sm"
        color="green"
        options={[
            { value: "s", label: "Small" },
            { value: "m", label: "Medium" },
            { value: "l", label: "Large" },
        ]}
    />
    ```
</PreviewCode>

<br/>
<br/>
<br/>
<br/>
<br/>
<br/>
