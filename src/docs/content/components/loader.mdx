---
title: "Loader"
description: "A loader component that displays a loading animation."
onThisPage:
  - title: "Overview"
    href: "#loader"
  - title: "Usage"
    href: "#usage"
  - title: "Props"
    href: "#props"
  - title: "Examples"
    href: "#examples"
---

<div id="loader" />

<br/>

<div className="flex flex-col justify-center items-center gap-10">
    <div className="flex items-center gap-8">
        <Loader type="spinner" color="primary" />
        <Loader type="spinner" color="green" />
        <Loader type="spinner" color="orange" />
    </div>
    <div className="flex items-center gap-8">
        <Loader type="dots" color="purple" />
        <Loader type="dots" color="red" />
        <Loader type="dots" color="pink" />
    </div>
</div>

<br/>

The `Loader` component is a loader component that displays a loading animation. It is a great way to show users that content is loading and to keep them engaged while they wait.

<Callout>
    Use the `type` prop to specify the type of loader to display. The `spinner` type displays a spinning circle loader, while the `dots` type displays a three-dot loader.
</Callout>

<br/>
<br/>
<Divider />

<div id="usage" />

## Usage

```js
import Loader from "@/components/atoms/Loader";

<Loader type="spinner" color="primary" />
```

<br/>
<br/>
<br/>
<Divider />

<div id="props" />

## Props

| Prop      | Type                | Default     | Description                                                                                                 |
|-----------|---------------------|-------------|-------------------------------------------------------------------------------------------------------------|
| type      | "spinner" \| "dots" | `"spinner"` | The type of loader to display. Possible values are `spinner` and `dots`.                                    |
| color     | string              | `"primary"` | [Tailwind](https://tailwindcss.com) color key used for the focus ring and border (`focus-within:border-*`). |
| className | string              | `""`        | Additional [Tailwind](https://tailwindcss.com) classes for the container.                                   |
| ...props  | object              | -           | Any other valid <code>&lt;div&gt;</code> attributes (e.g. `role`, `aria-*`, etc.).                          |

<br/>
<br/>
<br/>
<Divider />

<div id="examples" />

## Examples

### Spinner Loader
<PreviewCode preview={
    <div className="flex items-center gap-8">
        <Loader type="spinner" />
        <Loader type="spinner" color="green" />
    </div>
}>
```js
<Loader type="spinner" />
<Loader type="spinner" color="green" />
```
</PreviewCode>

### Dots Loader
<PreviewCode preview={
    <div className="flex items-center gap-8">
        <Loader type="dots" />
        <Loader type="dots" color="red" />
    </div>
}>
```js
<Loader type="dots" />
<Loader type="dots" color="red" />
```
</PreviewCode>

<br/>
<br/>